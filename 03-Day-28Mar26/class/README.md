# `03-Day-28Mar26/class` — full ML environment

This folder is a **uv** project (`pyproject.toml`, `uv.lock`).

## Setup

```powershell
cd 03-Day-28Mar26\class
uv venv
uv sync
```

Requires **Python 3.13+** per `pyproject.toml`.

See the [repository root README](../../README.md) for Jupyter kernels, API keys, and troubleshooting.
